apiVersion: 1

datasources:
  - name: VictoriaLogs
    type: victoriametrics-logs-datasource
    access: proxy
    url: http://host.docker.internal:9428/
    jsonData:
      timeField: "@timestamp"
      # 如果你的系统使用其他字段，如: "ts"
      # timeField: "ts"
    editable: true
    